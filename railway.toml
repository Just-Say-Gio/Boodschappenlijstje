[build]
builder = "nixpacks"
buildCommand = "npx tsx src/db/migrate.ts && npm run build:local"

[deploy]
startCommand = "node .next/standalone/server.js"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
numReplicas = 1
